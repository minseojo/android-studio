<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.minmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Minmin">
        <activity android:name=".game"></activity>
        <activity android:name=".movie"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".gameResult"></activity>
        <activity android:name=".gameList"></activity>
        <activity android:name=".movieList"></activity>
        <activity android:name=".movieResult"></activity>
        <activity android:name=".introActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>